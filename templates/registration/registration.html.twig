{% extends 'base.html.twig' %}

{% block title %}Hello RegistrationController!{% endblock %}

{% block body %}

<div class="row">
    <div class="col-6 mx-auto formulaire">
        {{ form_start(form) }}

        <div class="row">
            <div>{{ form_row(form.firstname) }}</div>
            <div>{{ form_row(form.lastname) }}</div>
        </div>
        <div class="row">
            <div class="col-6">{{ form_row(form.email) }}</div>
            <div class="col-6">{{ form_row(form.password) }}<br>
            </div>
        </div> 

        {{ form_end(form) }}
    </div>
</div>
<form>
{% endblock %}
